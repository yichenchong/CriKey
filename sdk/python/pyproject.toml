[project]
name = "crikey-sdk"
version = "0.1.0"
description = "Official Python SDK for CriKey plugins."
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["crikey_sdk"]
