; Keypirinha-style package configuration (spec 14.3), reported by the loader as
; a package resource.

[main]
; Package-relative resource holding one short description per catalog item, in
; publication order. Item *labels* are not configurable: one of them is the
; string the CLI's percent-encoding is tested against, and it has exactly one
; home, in the module.
description_resource = data/descriptions.txt

[diagnostics]
; This fixture exists for `crikey dev inspect-catalog`, not for the conformance
; suite: it publishes three items and answers no suggestions.
expected_items = 3
expected_verdict = pass
